---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Gracias - Printek">
  <!-- Hero Section with Thank You Message -->
  <section class="relative bg-gradient-to-br from-blue-50 to-indigo-100 py-32 overflow-hidden">

    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center">
        <!-- Main Message -->
        <h1 class="text-5xl md:text-6xl font-bold text-gray-900 mb-6 leading-tight">
          <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-indigo-600">
            ¡Gracias!
          </span>
        </h1>

        <p class="text-xl md:text-2xl text-gray-700 mb-8 font-light">
          Tu solicitud ha sido recibida exitosamente
        </p>

        <!-- Response Time Information -->
        <div class="bg-white/80 backdrop-blur-sm rounded-2xl p-8 shadow-xl border border-white/20 mb-8">
          <div class="flex items-center justify-center mb-4">
            <svg class="w-8 h-8 text-blue-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <h2 class="text-2xl font-semibold text-gray-900">Garantía de Respuesta Rápida</h2>
          </div>
          <p class="text-lg text-gray-700 mb-2">
            <strong>Te contactaremos dentro de 24 horas</strong> con tu cotización personalizada
          </p>
          <p class="text-gray-600 mb-6">
            Nuestros especialistas en adquisiciones ya están revisando tus requerimientos
          </p>
          
          <!-- Simple Timeline -->
          <div class="flex justify-center items-center space-x-4 text-sm text-gray-600">
            <span class="flex items-center">
              <span class="w-2 h-2 bg-blue-600 rounded-full mr-2"></span>
              Solicitud Recibida
            </span>
            <div class="w-8 h-px bg-gray-300"></div>
            <span class="flex items-center">
              <span class="w-2 h-2 bg-yellow-500 rounded-full mr-2 animate-pulse"></span>
              En Revisión
            </span>
            <div class="w-8 h-px bg-gray-300"></div>
            <span class="flex items-center">
              <span class="w-2 h-2 bg-gray-300 rounded-full mr-2"></span>
              Cotización Lista
            </span>
          </div>
        </div>

        <!-- Call to Action Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="/" class="inline-flex items-center justify-center px-8 py-3 bg-blue-600 text-white rounded-lg font-semibold hover:bg-blue-700 transform hover:scale-105 transition-all duration-200 shadow-lg hover:shadow-xl">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
            </svg>
            Volver al Inicio
          </a>
          
          <a href="/services" class="inline-flex items-center justify-center px-8 py-3 bg-white text-blue-600 rounded-lg font-semibold border-2 border-blue-600 hover:bg-blue-50 transform hover:scale-105 transition-all duration-200 shadow-lg hover:shadow-xl">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
            </svg>
            Ver Servicios
          </a>
        </div>
      </div>
    </div>
  </section>

<style>
  /* Additional animations */
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }
  
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }

  /* Smooth scroll behavior */
  html {
    scroll-behavior: smooth;
  }

  /* Enhanced hover effects */
  .hover-lift:hover {
    transform: translateY(-5px);
  }
</style>