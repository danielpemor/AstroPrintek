---
const services = [
  {
    icon: '🏭',
    title: 'Soluciones Técnicas de Abastecimiento Industrial',
    description: 'Identificamos problemas de abastecimiento como escasez, obsolescencia, calidad y proveedores inestables.',
    features: ['Homologación de componentes', 'Sustitución tecnológica', 'Integración de fabricantes','Fabricación de piezas a la medida']
  },
  {
    icon: '⚙️',
    title: 'Soporte Postventa',
    description: 'Actuamos como enlace entre el cliente y los fabricantes o distribuidores, gestionando fallas, traducciones técnicas y garantías.',
    features: ['Gestión de garantías', 'Traducciones técnicas', 'Soporte especializado']
  },
  {
    icon: '🔧',
    title: 'Entregas Urgentes',
    description: 'Respondemos con agilidad ante situaciones críticas, ya sea en la cadena de suministro o durante el proceso de producción.',
    features: ['Respuesta inmediata', 'Soluciones de emergencia', 'Continuidad operativa garantizada','Coordinación logística urgente']
  }
];
---

<section id="services" class="py-20 bg-industrial-50">
  <div class="container mx-auto px-4 lg:px-6">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-heading font-bold text-industrial-900 mb-6">
        Nuestras Soluciones
      </h2>
      <p class="text-xl text-industrial-600 max-w-3xl mx-auto leading-relaxed">
        Soluciones Técnicas de Abastecimiento Industrial
      </p>
    </div>

    <!-- Services Grid -->
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      {services.map((service, index) => (
        <div class="bg-white rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 p-8 group hover:transform hover:scale-105">
          <!-- Icon -->
          <div class="text-6xl mb-6 text-center group-hover:animate-pulse">
            {service.icon}
          </div>
          
          <!-- Content -->
          <h3 class="text-2xl font-heading font-semibold text-industrial-900 mb-4 text-center">
            {service.title}
          </h3>
          
          <p class="text-industrial-600 mb-6 leading-relaxed">
            {service.description}
          </p>
          
          <!-- Features -->
          <ul class="space-y-2">
            {service.features.map((feature) => (
              <li class="flex items-center text-industrial-700">
                <svg class="w-5 h-5 text-primary-600 mr-3 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                {feature}
              </li>
            ))}
          </ul>
          
          <!-- CTA -->
          <div class="mt-6 text-center">
            <a 
              href="/services"
              class="text-primary-600 hover:text-primary-700 font-semibold transition-colors duration-200 group-hover:underline"
            >
              Más información →
            </a>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>